# cleanery 프로젝트 안내

> 이 파일은 AI 코딩 도구(Claude Code 등)가 프로젝트를 빠르게 이해하도록 돕는 안내서입니다.
> 작업하다 규칙이 생기면 한 줄씩 자유롭게 추가하세요.

## 이 프로젝트가 뭔가요

카카오톡 청소 견적 상담봇 (크리너리)

## 할 일

> 할 일 목록은 `TODO.md`에서 관리합니다.

## 📋 TODO.md 관리 규칙 (v2.1 · 2026-08-22 확정 · 전 저장소 공통)

- 구조는 두 섹션만 사용: `## 🔴 미해결`, `## ✅ 완료 로그`. **날짜별 섹션 헤더는 만들지 않는다**
- 새 할 일은 `🔴 미해결` 섹션 **맨 위**에 항목만 추가. 등록일은 항목 옆 괄호로: `(등록: YYYY-MM-DD)`. 미해결 항목에는 담당자를 적지 않는다
- 완료된 항목은 취소선으로 남기지 말고 `✅ 완료 로그` 섹션 **맨 위**로 옮길 것. 순서는 **날짜 → 작업자 → 내용 → 커밋**:
  `- [x] YYYY-MM-DD, 작업자 — 항목 내용 (커밋 abc1234)`
- **작업자를 반드시 적는다** — 클로드코드 / 코덱스 / 별이 / 초롱이 / 반짝이 / n8n / 아빠 / 김영재 등. 여럿이면 쉼표로 나열한다
  (작업자가 비어 있는 항목은 이 규칙 도입 2026-08-22 이전 기록이라 소급 표기하지 않은 것)
- 커밋 해시가 없으면 `(커밋 abc1234)` 부분은 생략한다
- **완료 로그는 다른 파일로 옮기지 않고 무기한 누적한다.** 예전의 "완료 로그 15개 초과 시 `CHANGELOG.md`로 이관" 규칙은 폐기됨
- 같은 항목이 두 섹션에 중복 등장하지 않게 할 것

> **날짜 기준**: 하루의 경계는 자정이 아니라 **한국시간 오전 9시**. 오전 9시부터 다음날 오전 8시 59분까지가 하나의 "작업일"이며, 자정을 넘겨 작업했어도 아직 오전 9시 전이면 **전날 날짜**로 기록한다. (2026-08-11 확인)

> **AI 도구 작업 후 처리 (2026-08-22 확정)**: 코덱스 등 AI 도구가 작업했으면 **커밋·푸시까지 마치고 `TODO.md` 완료 로그에 작업자와 함께 남긴다.** 작업만 하고 커밋을 안 하면 그 PC 밖에서는 내용이 사라지고, 다른 도구가 같은 파일을 건드려 충돌한다.

